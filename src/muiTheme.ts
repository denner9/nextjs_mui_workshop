'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#048853',
      light: '#F3FFF4',
      dark:'#035F3A',
    }
  }
});

export default theme;